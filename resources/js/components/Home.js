import React, { useEffect, useRef } from 'react'

export const Home = () => {
  const data= useRef("data")
  useEffect(() => {
    
  
    return () => {
    
    }
  }, [])
  
  return (
    <div>Home</div>
  )
}
